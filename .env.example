jdbc.url=
jdbc.username=
jdbc.password=
# Copy to src/main/resources/config.properties (gitignored for production secrets)
# Local H2 needs no real secret; still do not commit production credentials.
